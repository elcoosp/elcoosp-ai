name: Release and Publish

on:
  push:
    tags:
      - "v*"

jobs:
  release-and-publish:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      contents: write
      id-token: write
      pull-requests: read
    defaults:
      run:
        working-directory: ./seedling

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: "./seedling/pnpm-lock.yaml"

      - run: pnpm install
      - run: pnpm run prepublishOnly

      - name: Generate Changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@v6
        with:
          configuration: ".github/changelog-config.json"
          commitMode: "conventional"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Write changelog to a file inside seedling/ (because of working-directory)
      - name: Write Changelog to File
        run: echo "${{ steps.changelog.outputs.changelog }}" > CHANGELOG.md

      # Create GitHub Release – note body_path points to seedling/CHANGELOG.md
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: seedling ${{ github.ref_name }}
          body_path: seedling/CHANGELOG.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to npm
        run: pnpm publish --access public --no-git-checks
